{
  "name": "my-app",
  "private": true,
  "scripts": {
    "build": "npm install --prefix react-frontend && npm install --prefix backend",
    "start": "concurrently \"npm start --prefix react-frontend\" \"npm run dev --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}